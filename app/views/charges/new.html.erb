<% if current_user.role != 'premium' %>
 <%= form_tag charges_path do %>
   <h2>Click Below and Pay to become a Premium Member! Only $50!</h2>
   <h4>Benefits include:</h4>
   <ul> 
     <li>Satisfaction of being an elite premium member</li>
     <li>Street Cred</li>
     <li>Ability to create Private Wikis</li>
   </ul>
   <script class='stripe-button' src="https://checkout.stripe.com/checkout.js" data-key="<%= @stripe_btn_data[:key] %>" data-amount="<%= @stripe_btn_data[:amount] %>" data-description="<%= @stripe_btn_data[:description] %>" ></script>
 <% end %>
 
<% else %>
 <h4>You don't have to do this! But there are no refunds... so whatever. Fair warning though: all of your private wikis will become public. You still sure?</h4>
 <%= button_to "Downgrade", {action: "downgrade"}, method: :get %>
<% end %>
